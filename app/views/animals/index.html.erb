<p><%= @animals.count %> pets to rent</p>
<% @animals.each do |animal| %>
  <div>
    <%= image_tag animal.photo %>
    <%= animal.name %>
    <%= animal.species %>
    <p><%= animal.price %>/ night</p>
  </div>
<% end %>
