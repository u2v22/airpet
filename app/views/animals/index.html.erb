<div class="container-index">
  <div class="all-wrapper">
    <div class="animal-wrapper container">
      <p><%= @animals.count %> pets to foster</p>
      <div class='cards-animal-index'>
        <% @animals.each do |animal| %>
        <%= link_to animal_path(animal) do %>
          <div class="card-animal-trip">
            <%= cl_image_tag animal.photo %>
             <div class="card-animal-trip-infos">
               <div>
                 <h2><%= animal.name %></h2>
                 <p><%= animal.species %></p>
               </div>
               <h2 class="card-trip-pricing"><%= animal.price %>€ / day</h2>
             </div>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
    <div
      id="map";
      class="map-wrapper";
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
    ></div>
  </div>
</div>
